<?php require("template/CustomerNavBar.phtml"); ?>

<div class="container py-5">
    <h2 class="text-white mb-4">My Booking Requests</h2>

    <!-- Filters -->
    <form method="get" class="row g-3 mb-4">
        <div class="col-md-4">
            <select name="status" class="form-select">
                <option value="">Filter by Status</option>
                <option value="1" <?= $_GET['status'] ?? '' === '1' ? 'selected' : '' ?>>Pending</option>
                <option value="2" <?= $_GET['status'] ?? '' === '2' ? 'selected' : '' ?>>Approved</option>
                <option value="3" <?= $_GET['status'] ?? '' === '3' ? 'selected' : '' ?>>Declined</option>
            </select>
        </div>
        <div class="col-md-4">
            <select name="sort" class="form-select">
                <option value="newest" <?= $_GET['sort'] ?? '' === 'newest' ? 'selected' : '' ?>>Sort by Newest</option>
                <option value="oldest" <?= $_GET['sort'] ?? '' === 'oldest' ? 'selected' : '' ?>>Sort by Oldest</option>
            </select>
        </div>
        <div class="col-md-4">
            <button type="submit" class="btn btn-dark w-100">Apply Filters</button>
        </div>
    </form>

    <!-- Booking Cards -->
    <?php if (empty($view->bookings)): ?>
        <div class="alert alert-light">No bookings found.</div>
    <?php else: ?>
        <div class="row g-4">
            <?php foreach ($view->bookings as $b): ?>
                <?php
                $statusId = (int) $b['Booking_status_ID'];
                $statusText = $b['Booking_status'];
                $badge = match ($statusId) {
                    1 => 'warning',
                    2 => 'success',
                    3 => 'danger',
                    default => 'secondary'
                };
                $statusClass = match ($statusId) {
                    1 => 'status-pending',
                    2 => 'status-approved',
                    3 => 'status-declined',
                    default => ''
                };

                $start = new DateTime($b['Booking_start']);
                $end   = new DateTime($b['Booking_end']);
                $duration = $start->diff($end);
                $kwh = $duration->h + ($duration->i >= 30 ? 1 : 0);
                $cost = number_format($kwh * $b['Price_per_kwatt'], 2);
                ?>
                <div class="col-md-4">
                    <div class="card card-hover bg-transparent border-0 p-0 <?= $statusClass ?>" onclick="loadChargerDetails(<?= $b['Charger_point_ID'] ?>)">
                        <div class="card-inner">
                            <!-- Front -->
                            <div class="card-front card-body border">
                                <h5 class="card-title">⚡ Booking on <?= $start->format('Y-m-d') ?></h5>
                                <p class="card-text"><strong>Start:</strong> <?= $start->format('H:i') ?></p>
                                <p class="card-text"><strong>End:</strong> <?= $end->format('H:i') ?></p>
                                <p class="card-text">
                                    <strong>Status:</strong>
                                    <span class="badge bg-<?= $badge ?>"><?= strtoupper($statusText) ?></span>
                                </p>
                                <p class="card-text"><strong>kWh:</strong> <?= $kwh ?></p>
                                <p class="card-text"><strong>Cost:</strong> <?= $cost ?> BD</p>
                            </div>

                            <!-- Back -->
                            <div class="card-back">
                                <p class="m-0">Click to view Charger Point Details</p>
                            </div>
                        </div>
                    </div>
                </div>

            <?php endforeach; ?>
        </div>


        <!-- Pagination -->
        <nav class="mt-4">
            <ul class="pagination justify-content-center">
                <?php if ($view->currentPage > 1): ?>
                    <li class="page-item">
                        <a class="page-link" href="?page=<?= $view->currentPage - 1 ?>&status=<?= $_GET['status'] ?? '' ?>&sort=<?= $_GET['sort'] ?? '' ?>">Previous</a>
                    </li>
                <?php else: ?>
                    <li class="page-item disabled"><span class="page-link">Previous</span></li>
                <?php endif; ?>

                <?php for ($i = 1; $i <= $view->totalPages; $i++): ?>
                    <li class="page-item <?= $i === $view->currentPage ? 'active' : '' ?>">
                        <a class="page-link" href="?page=<?= $i ?>&status=<?= $_GET['status'] ?? '' ?>&sort=<?= $_GET['sort'] ?? '' ?>"><?= $i ?></a>
                    </li>
                <?php endfor; ?>

                <?php if ($view->currentPage < $view->totalPages): ?>
                    <li class="page-item">
                        <a class="page-link" href="?page=<?= $view->currentPage + 1 ?>&status=<?= $_GET['status'] ?? '' ?>&sort=<?= $_GET['sort'] ?? '' ?>">Next</a>
                    </li>
                <?php else: ?>
                    <li class="page-item disabled"><span class="page-link">Next</span></li>
                <?php endif; ?>
            </ul>
        </nav>
    <?php endif; ?>
</div>

<script src="/js/UserBooksJS.js"></script>
<?php require("template/footer.phtml"); ?>
